<div class="modal-header">
  <h3>Delete skill on profile</h3>
</div>
<div class="modal-body">
  <ul class="errors"></ul>
  Are you sure you want to remove <strong><%= @skills_user.skill.name %></strong> from your list of skills?
</div>
<div class="modal-footer">
  <%= link_to 'Yes delete this skill?', skill_path(@skills_user), method: :delete, remote: true, class: 'btn btn-danger' %>
  <%= link_to 'No please don\'t', "#", class: 'btn btn-default', data: { dismiss: "modal" } %>
</div>